<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/friendProfileRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:padding="24dp"
    android:background="@drawable/profile_bg_gradient"
    tools:context=".FriendProfileActivity">

    <!-- Top bar: back + title -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <ImageButton
            android:id="@+id/FriendBackButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@android:color/transparent"
            android:contentDescription="Back"
            android:src="@android:drawable/ic_media_previous"
            app:tint="#E8F5E9" />

        <TextView
            android:id="@+id/FriendProfileTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="Friend Profile"
            android:textColor="#E8F5E9"
            android:textSize="24sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Card for friend's stats + history -->
    <LinearLayout
        android:id="@+id/friendProfileCard"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:padding="20dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp"
        android:background="@drawable/profile_card_bg"
        android:elevation="6dp">

        <TextView
            android:id="@+id/FriendUsernameDisplay"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Friend Username"
            android:textAlignment="center"
            android:textSize="22sp"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="8dp" />

        <TextView
            android:id="@+id/FriendPlantCounterText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Friend has identified 0 plants!"
            android:textAlignment="center"
            android:textColor="#A5D6A7"
            android:textSize="15sp"
            android:layout_marginBottom="4dp" />

        <TextView
            android:id="@+id/FriendPlantRankingText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Friend is a Plant Beginner"
            android:textAlignment="center"
            android:textColor="#A5D6A7"
            android:textSize="15sp"
            android:layout_marginBottom="12dp" />

        <ProgressBar
            android:id="@+id/FriendRankProgressBar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="8dp"
            android:max="100"
            android:progress="0"
            android:progressDrawable="@drawable/green_progress_bg"
            android:layout_marginBottom="16dp" />

        <TextView
            android:id="@+id/FriendHistoryTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Plant History"
            android:textColor="#E8F5E9"
            android:textStyle="bold"
            android:textSize="18sp"
            android:layout_marginBottom="6dp" />

        <ListView
            android:id="@+id/FriendHistoryListView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@android:color/transparent"
            android:padding="0dp"
            android:divider="#264935"
            android:dividerHeight="1dp"
            android:scrollbars="vertical" />

    </LinearLayout>

    <!-- Remove friend button -->
    <Button
        android:id="@+id/RemoveFriendButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Remove Friend"
        android:textStyle="bold"
        android:textColor="@android:color/white"
        android:backgroundTint="#E53935"
        android:layout_marginBottom="4dp" />

</LinearLayout>
