<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:padding="24dp"
    android:background="@drawable/profile_bg_gradient"
    tools:context=".SettingsActivity">

    <!-- Top bar row: Back + Title + Friends button with badge -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <ImageButton
            android:id="@+id/BackButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@android:color/transparent"
            android:contentDescription="Back"
            android:src="@android:drawable/ic_media_previous"
            app:tint="#E8F5E9" />

        <TextView
            android:id="@+id/textViewTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="Profile"
            android:textColor="#E8F5E9"
            android:textSize="28sp"
            android:textStyle="bold" />

        <!-- Friends button wrapped so we can overlay a badge -->
        <FrameLayout
            android:layout_width="40dp"
            android:layout_height="40dp">

            <ImageButton
                android:id="@+id/AddFriendButton"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@android:color/transparent"
                android:contentDescription="Friends"
                android:src="@android:drawable/ic_input_add"
                app:tint="#E8F5E9" />

            <TextView
                android:id="@+id/friendRequestBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="top|end"
                android:layout_marginTop="2dp"
                android:layout_marginEnd="2dp"
                android:background="@drawable/badge_background"
                android:paddingLeft="4dp"
                android:paddingRight="4dp"
                android:paddingTop="1dp"
                android:paddingBottom="1dp"
                android:minWidth="16dp"
                android:minHeight="16dp"
                android:gravity="center"
                android:text="0"
                android:textSize="10sp"
                android:textColor="@android:color/white"
                android:visibility="gone" />
        </FrameLayout>
    </LinearLayout>

    <!-- Card container for the content -->
    <LinearLayout
        android:id="@+id/profileCard"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:padding="20dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp"
        android:background="@drawable/profile_card_bg"
        android:elevation="6dp">

        <TextView
            android:id="@+id/UsernameDisplay"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Username"
            android:textAlignment="center"
            android:textSize="22sp"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="8dp" />

        <TextView
            android:id="@+id/PlantCounterText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="You've identified 0 plants!"
            android:textAlignment="center"
            android:textColor="#A5D6A7"
            android:textSize="15sp"
            android:layout_marginBottom="4dp" />

        <TextView
            android:id="@+id/PlantRankingText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="You are a Plant Amateur"
            android:textAlignment="center"
            android:textColor="#A5D6A7"
            android:textSize="15sp"
            android:layout_marginBottom="12dp" />

        <ProgressBar
            android:id="@+id/RankProgressBar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="8dp"
            android:max="100"
            android:progress="30"
            android:progressDrawable="@drawable/green_progress_bg"
            android:layout_marginBottom="16dp" />

        <!-- Plant History section -->
        <TextView
            android:id="@+id/PlantHistoryTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Plant History"
            android:textColor="#E8F5E9"
            android:textStyle="bold"
            android:textSize="18sp"
            android:layout_marginBottom="6dp" />

        <!-- EMPTY VIEW for history -->
        <TextView
            android:id="@+id/historyEmptyText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Your plant history is empty. Capture your first plant to get started!"
            android:textColor="#B0BEC5"
            android:textSize="14sp"
            android:gravity="center"
            android:paddingTop="8dp"
            android:paddingBottom="8dp"
            android:visibility="gone" />

        <ListView
            android:id="@+id/listView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@android:color/transparent"
            android:padding="0dp"
            android:divider="#264935"
            android:dividerHeight="1dp"
            android:scrollbars="vertical" />

        <!-- Friends section under plant history -->
        <TextView
            android:id="@+id/FriendsTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Friends"
            android:textColor="#E8F5E9"
            android:textStyle="bold"
            android:textSize="18sp"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="6dp" />

        <!-- EMPTY VIEW for friends -->
        <TextView
            android:id="@+id/friendsEmptyText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Add a friend with the 'plus' icon in the top right corner!"
            android:textColor="#B0BEC5"
            android:textSize="14sp"
            android:gravity="center"
            android:paddingTop="8dp"
            android:paddingBottom="8dp"
            android:visibility="gone" />

        <ListView
            android:id="@+id/friendsListView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:padding="0dp"
            android:divider="#264935"
            android:dividerHeight="1dp"
            android:scrollbars="vertical" />

    </LinearLayout>

    <!-- Bottom buttons -->
    <Button
        android:id="@+id/ClearHistoryButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Clear History"
        android:backgroundTint="@android:color/transparent"
        android:textColor="#6EF7A7"
        android:textStyle="bold"
        android:layout_marginBottom="8dp"
        android:foreground="?attr/selectableItemBackground"
        android:padding="10dp" />

    <Button
        android:id="@+id/LogoutButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="4dp"
        android:backgroundTint="#47D04C"
        android:text="Log Out"
        android:textColor="#001018"
        android:textStyle="bold" />

</LinearLayout>
